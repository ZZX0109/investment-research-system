[pytest]
# Pytest 配置文件
testpaths = backend/tests e2e
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    ml: wraps legacy ml/tests run() smoke checks
